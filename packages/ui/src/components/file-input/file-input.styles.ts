import { createStyles } from '@/styles';

export default createStyles(() => ({
  classes: {
    placeholder: '',
    input: 'cursor-pointer',
  },
  styles: {
    placeholder: {},
    input: {},
  },
}));
